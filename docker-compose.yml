version: '3.8'

services:
  client:
    image: registry.digitalocean.com/<your-registry-name>/book-dragon-client:latest
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: book-dragon-client
    restart: unless-stopped
    ports:
      - "127.0.0.1:56909:80"
