version: '3.8'

services:
  client:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: book-dragon-client
    restart: unless-stopped
    ports:
      - "127.0.0.1:56909:80"
